<!DOCTYPE html>
<html>
<head>
    <title>{{ reportType }}</title>
    <style>
        .report-container {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 20px;
        }
        .report-container .header {
            color: #333;
        }
        .report-container .paragraph {
            margin: 5px 0;
        }
        .report-container .box {
            border: 1px solid #ccc;
            border-radius: 5px;
            padding: 15px;
            margin-bottom: 20px;
            background-color: #f9f9f9;
        }
        .report-container .list {
            list-style-type: none;
            padding: 0;
        }
        .report-container .list-item {
            border-bottom: 1px solid #ddd;
            padding: 10px 0;
        }
    </style>
</head>
<body class="report-container">
<h1 class="header">{{ reportType }}</h1>
<p class="paragraph">Generated At: {{ generatedAt }}</p>
<p class="paragraph">Number of Flights: {{ numberOfFlights }}</p>

<h2 class="header">Flights</h2>
{% for flight in flights %}
    <div class="box">
        <h3 class="header">Flight Number: {{ flight.flightNumber }}</h3>
        <p class="paragraph">Origin: {{ flight.origin }}</p>
        <p class="paragraph">Destination: {{ flight.destination }}</p>
        <p class="paragraph">Departure Time: {{ flight.departureTime }}</p>
        <p class="paragraph">Arrival Time: {{ flight.arrivalTime }}</p>
        <p class="paragraph">Base Price (Economy): {{ flight.basePriceEconomy }}</p>
        <p class="paragraph">Base Price (Business): {{ flight.basePriceBusiness }}</p>
        <p class="paragraph">Base Price (First Class): {{ flight.basePriceFirstClass }}</p>
        <p class="paragraph">Total Seats: {{ flight.totalSeats }}</p>
        <p class="paragraph">Available Seats (Economy): {{ flight.availableSeatsEconomy }}</p>
        <p class="paragraph">Available Seats (Business): {{ flight.availableSeatsBusiness }}</p>
        <p class="paragraph">Available Seats (First Class): {{ flight.availableSeatsFirstClass }}</p>
        <p class="paragraph">Number of Tickets Sold: {{ flight.numberOfTicketsSold }}</p>
        <p class="paragraph">Total Revenue Expected: {{ flight.totalRevenueExpected }}</p>
        <p class="paragraph">Total Revenue Collected: {{ flight.totalRevenueCollected }}</p>
        <p class="paragraph">Total Revenue Reserved: {{ flight.totalRevenueReserved }}</p>

        {% for ticket in flight.tickets %}
            {% if ticket.passengerName and ticket.seatClass and ticket.priceFinalCents %}
                <li class="list-item">
                    <p class="paragraph">Passenger Name: {{ ticket.passengerName }}</p>
                    <p class="paragraph">Seat Class: {{ ticket.seatClass }}</p>
                    <p class="paragraph">Price Final: {{ ticket.priceFinalCents }}</p>
                    <p class="paragraph">Reservation Status: {{ ticket.reservationStatus }}</p>
                    <p class="paragraph">Passenger DOB: {{ ticket.passengerDob }}</p>
                </li>
            {% endif %}
        {% endfor %}
    </div>
{% endfor %}
</body>
</html>